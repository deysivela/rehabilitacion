{
  "name": "rehabilitacion",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node backend/index.js",
    "build": "cd frontend && npm install && npm run build && cp -r build ../backend/frontend"
  }
}
